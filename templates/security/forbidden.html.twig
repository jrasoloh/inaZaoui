{% extends 'base.html.twig' %}

{% block title %}Accès refusé — Ina Zaoui{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="mx-auto text-center" style="max-width: 560px;">
            <p class="display-1 mb-0">403</p>
            <h1 class="h3 mb-3">Accès refusé</h1>
            <p class="text-muted">
                Cette section est réservée à l’administratrice du site. Votre
                compte ne dispose pas des droits nécessaires pour y accéder.
            </p>
            <div class="d-flex justify-content-center gap-2 mt-4">
                <a href="{{ path('admin_media_index') }}" class="btn btn-primary">Retour à mes médias</a>
                <a href="{{ path('home') }}" class="btn btn-outline-secondary">Aller à l’accueil</a>
            </div>
        </div>
    </div>
{% endblock %}

